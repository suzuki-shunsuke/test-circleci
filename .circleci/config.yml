version: 2.1

orbs:
  aqua: aquaproj/aqua@dev:d11d0416e3b33d5f0c7cdb34c40e29abb91842f0

workflows:
  build:
    jobs:
      - foo
jobs:
  foo:
    docker:
      - image: alpine:3.18.2
    steps:
      - run:
          command: |
            apk add git curl bash
      - checkout
      - aqua/aqua:
          aqua_version: v2.9.0
      - run:
          shell: /bin/bash -euo pipefail
          command: |
            command -v circleci
            circleci version
