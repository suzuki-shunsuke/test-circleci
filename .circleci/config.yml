---
jobs:
  build:
    docker:
      - image: alpine:3.10.3
    steps:
      - checkout
      - run: |
          sleep 3
          echo build
  test:
    docker:
      - image: alpine:3.10.3
    steps:
      - checkout
      - run: |
          sleep 5
          echo test
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test
