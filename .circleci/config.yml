version: 2.1

orbs:
  aqua: aquaproj/aqua@dev:f18fe08175cdfbb9c94abb8b62749d14b152dea0

workflows:
  build:
    jobs:
      - foo
jobs:
  foo:
    docker:
      - image: alpine:3.18.2
    steps:
      - run:
          command: |
            apk add git curl
      - checkout
      - aqua/aqua:
          aqua_version: v2.9.0
      - run:
          command: |
            command -v circleci
            circleci version
