version: 2.1
orbs:
  git-shallow-clone: guitarrapc/git-shallow-clone@1.0.1
workflows:
  build:
    jobs:
      - build
jobs:
  build:
    docker:
      - image: plugins/git
    steps:
      - git-shallow-clone/checkout_shallow
