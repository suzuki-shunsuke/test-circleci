version: 2.1

orbs:
  aqua: aquaproj/aqua@dev:adffd79b317d82ccfe73900b99cc32ebe819ae92

workflows:
  build:
    jobs:
      - foo
jobs:
  foo:
    docker:
      - image: alpine:3.18.2
    steps:
      - run:
          command: |
            apk add git curl
      - checkout
      - aqua/aqua:
          aqua_version: v2.9.0
      - run:
          command: |
            command -v circleci
            circleci version
